<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- עדכון מתוך האפליקציה: מאפשר להפעיל את מתקין המערכת על ה-APK שהורדנו,
         בלי לצאת לדפדפן/גיטהאב. אנדרואיד 8+ עדיין דורש אישור חד-פעמי מהמשתמש. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android TV: לא דורש מסך מגע / leanback, כדי שגם טלוויזיות יוכלו להתקין -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".MainApplication"
        android:label="ZOVEX"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:banner="@drawable/tv_banner">

        <!-- מכבים את האתחול-האוטומטי של FCM בעלייה. על מכשירים בלי Google Play
             Services (Qin F22/F21 Pro, טלוויזיות מסוימות) האתחול האוטומטי מנסה
             לפנות ל-GMS בזמן ה-boot ועלול להקריס. במקום זה מפעילים את FCM ידנית
             מ-JS רק אחרי שווידאנו ש-GMS קיים (setAutoInitEnabled(true)). -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false"
            tools:replace="android:value" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false"
            tools:replace="android:value" />

        <!-- FileProvider: נדרש כדי למסור את קובץ העדכון (APK) למתקין המערכת
             דרך content:// . בלי זה אנדרואיד זורק FileUriExposedException. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Chromecast: מצביע על ה-OptionsProvider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.zovexapp.CastOptionsProvider" />

        <activity
            android:name=".MainActivity"
            android:label="ZOVEX"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:supportsPictureInPicture="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="IOS_SCHEME_PLACEHOLDER" />
            </intent-filter>

            <!-- Custom-scheme deep link: zovex://<slug> opens the app directly,
                 works immediately, no domain verification needed. Good for
                 links shared over WhatsApp/Telegram/email. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zovex" />
            </intent-filter>

            <!-- Android App Links: makes a normal https://davidggjg.github.io/zovex/...
                 link open this app directly instead of a browser, IF the domain
                 ownership is verified (assetlinks.json hosted at the domain root -
                 see docs/app-links-setup.md for the remaining manual step). Until
                 that file is in place, Android just falls back to opening the link
                 in the browser as normal - this doesn't break anything either way. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="davidggjg.github.io"
                    android:pathPrefix="/zovex" />
            </intent-filter>
        </activity>
    </application>

</manifest>
